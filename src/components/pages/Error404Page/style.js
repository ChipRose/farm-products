import styled from "styled-components";
import { Container } from "/src/components/styled";

const Error404PageContainer = styled(Container)`
  padding: 10px 0;
  text-align: center;
`;

export { Error404PageContainer };
